//20. (**) Підрахувати суму чисел у заданому діапазоні. Наприклад, при вхідних даних 50 та 52повинно бути 50 + 51 + 52 = 153.
int main() {
    int a =52; //створюємо початкове число діапазону припустимо це 52 
    int b = 56;  // створюємо кінцеве число діапазону
    int c = (b - a + 1)*(a+b)/2; // формула з арифметичної прогресії яку зручно використати  для знаходження суми діапазону
    return 0;
}
