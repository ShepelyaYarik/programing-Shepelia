Шепеля Ярослав Миколайович КН-925а
1)визначаємось з тим як будемо писать код та яку для себе ставимо задачу
2)добавляємо всі потрібні бібліотеки, а саме#include <stdlib.h> #include <time.h> #include <stdio.h>
3) робимо функцію для обрахунку суми масиву 
4) далі добавляєм мінімальне та максимальне значення для діапазону рандомних чисел
5) добавляємо логіку перевірки аргументів які можуть бути введені через термінал
6) в разі надходження аргументів використвуємо strtol як альтернативу atoi для переведення аргументів які надійшли як рядок в числове значення
7) пишемо цикл для заповнення масиву рандомними значеннями в заданому діапазоні
8) вносимо масив в функцію і через функцію виведення  ми бачимо результат суми
